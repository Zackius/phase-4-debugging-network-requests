// Generated by CoffeeScript 1.7.1
(function() {
  var all, commands, dynamic, everySecond, evil, exceptFirst, exceptLast, first, keys, migrate, multi, prefixKeys, prefixer, proxies, sort,
    __slice = [].slice,
    __indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  commands = require('redis/lib/commands');

  first = {
    proxy: function(command, prefix, db) {
      return function() {
        var args, key;
        key = arguments[0], args = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
        return db[command].apply(db, [prefix + key].concat(__slice.call(args)));
      };
    },
    commands: ['append', 'decr', 'decrby', 'dump', 'exists', 'expire', 'expireat', 'get', 'getbit', 'getrange', 'getset', 'hdel', 'hexists', 'hget', 'hgetall', 'hincrby', 'hincrbyfloat', 'hkeys', 'hlen', 'hmget', 'hmset', 'hset', 'hsetnx', 'hvals', 'incr', 'incrby', 'incrbyfloat', 'lindex', 'linsert', 'llen', 'lpop', 'lpush', 'lpushx', 'lrange', 'lrem', 'lset', 'ltrim', 'move', 'persist', 'pexpire', 'pexpireat', 'psetex', 'pttl', 'publish', 'restore', 'rpop', 'rpush', 'rpushx', 'sadd', 'scard', 'set', 'setbit', 'setex', 'setnx', 'setrange', 'sismember', 'smembers', 'spop', 'srandmember', 'srem', 'strlen', 'ttl', 'type', 'zadd', 'zcard', 'zcount', 'zincrby', 'zrange', 'zrangebyscore', 'zrank', 'zrem', 'zremrangebyrank', 'zremrangebyscore', 'zrevrange', 'zrevrangebyscore', 'zrevrank', 'zscore']
  };

  all = {
    proxy: function(command, prefix, db) {
      return function() {
        var keys, prefixed;
        keys = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
        prefixed = prefixKeys(prefix, keys);
        return db[command].apply(db, prefixed);
      };
    },
    commands: ['del', 'psubscribe', 'punsubscribe', 'subscribe', 'unsubscribe', 'watch', 'rpoplpush', 'mget', 'rename', 'renamenx', 'sdiff', 'sdiffstore', 'sinter', 'sinterstore', 'sunion', 'sunionstore']
  };

  exceptFirst = {
    proxy: function(command, prefix, db) {
      return function() {
        var arg, keys, prefixed;
        arg = arguments[0], keys = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
        prefixed = prefixKeys(prefix, keys);
        return db[command].apply(db, [arg].concat(__slice.call(prefixed)));
      };
    },
    commands: ['bitop']
  };

  exceptLast = {
    proxy: function(command, prefix, db) {
      return function() {
        var arg, cb, keys, prefixed, _i;
        keys = 3 <= arguments.length ? __slice.call(arguments, 0, _i = arguments.length - 2) : (_i = 0, []), arg = arguments[_i++], cb = arguments[_i++];
        if (typeof cb === 'function') {
          prefixed = prefixKeys(prefix, keys);
          return db[command].apply(db, __slice.call(prefixed).concat([arg], [cb]));
        } else {
          keys.push(arg);
          arg = cb;
          prefixed = prefixKeys(prefix, keys);
          return db[command].apply(db, __slice.call(prefixed).concat([arg]));
        }
      };
    },
    commands: ['blpop', 'brpop', 'brpoplpush', 'smove']
  };

  everySecond = {
    proxy: function(command, prefix, db) {
      return function() {
        var arg, args, index, prefixed, _i, _len;
        args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
        prefixed = [];
        for (index = _i = 0, _len = args.length; _i < _len; index = ++_i) {
          arg = args[index];
          if (index % 2 || index === args.length - 1) {
            prefixed.push(arg);
          } else {
            prefixed.push(prefix + arg);
          }
        }
        return db[command].apply(db, prefixed);
      };
    },
    commands: ['mset', 'msetnx']
  };

  migrate = {
    proxy: function(command, prefix, db) {
      return function(host, port, key, dest, timeout, next) {
        return db[command](host, port, prefix + key, dest, timeout, next);
      };
    },
    commands: ['migrate']
  };

  dynamic = {
    proxy: function(command, prefix, db) {
      return function() {
        var args, dest, keys, numKeys, prefixed;
        dest = arguments[0], numKeys = arguments[1], args = 3 <= arguments.length ? __slice.call(arguments, 2) : [];
        keys = args.splice(0, numKeys);
        prefixed = prefixKeys(prefix, keys);
        return db[command].apply(db, [prefix + dest, numKeys].concat(__slice.call(prefixed), __slice.call(args)));
      };
    },
    commands: ['zinterstore', 'zunionstore']
  };

  evil = {
    proxy: function(command, prefix, db) {
      return function() {
        var args, keys, numKeys, prefixed, script;
        script = arguments[0], numKeys = arguments[1], args = 3 <= arguments.length ? __slice.call(arguments, 2) : [];
        keys = args.splice(0, numKeys);
        prefixed = prefixKeys(prefix, keys);
        return db[command].apply(db, [script, numKeys].concat(__slice.call(prefixed), __slice.call(args)));
      };
    },
    commands: ['eval']
  };

  sort = {
    proxy: function(command, prefix, db) {
      return function() {
        var arg, args, key, lastArg, prefixed, _i, _len;
        key = arguments[0], args = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
        prefixed = [];
        lastArg = '';
        for (_i = 0, _len = args.length; _i < _len; _i++) {
          arg = args[_i];
          if (lastArg.toLowerCase() === 'get') {
            prefixed.push(prefix + arg);
          } else {
            prefixed.push(arg);
          }
          lastArg = arg;
        }
        return db[command].apply(db, [prefix + key].concat(__slice.call(prefixed)));
      };
    },
    commands: ['sort']
  };

  keys = {
    proxy: function(command, prefix, db) {
      return function(pattern, next) {
        if (!next) {
          return;
        }
        return db[command](prefix + pattern, function(err, result) {
          var k;
          if (err) {
            return next(err);
          }
          return next(null, (function() {
            var _i, _len, _results;
            _results = [];
            for (_i = 0, _len = result.length; _i < _len; _i++) {
              k = result[_i];
              _results.push(k.substring(prefix.length));
            }
            return _results;
          })());
        });
      };
    },
    commands: ['keys']
  };

  multi = {
    proxy: function(command, prefix, db) {
      return function() {
        return prefixer(db.multi(), true)(prefix);
      };
    },
    commands: ['multi']
  };

  proxies = [first, all, exceptFirst, exceptLast, everySecond, migrate, dynamic, evil, sort, keys, multi];

  prefixKeys = function(prefix, keys) {
    var key, prefixed;
    return prefixed = (function() {
      var _i, _len, _results;
      _results = [];
      for (_i = 0, _len = keys.length; _i < _len; _i++) {
        key = keys[_i];
        if (typeof key === 'function') {
          _results.push(key);
        } else {
          _results.push(prefix + key);
        }
      }
      return _results;
    })();
  };

  module.exports = prefixer = function(db, chain) {
    return function(prefix) {
      var command, p, proxy, proxyCommand, _i, _j, _len, _len1;
      proxy = {};
      proxyCommand = function(command) {
        return proxy[command] = function() {
          return db[command].apply(db, arguments);
        };
      };
      for (_i = 0, _len = commands.length; _i < _len; _i++) {
        command = commands[_i];
        for (_j = 0, _len1 = proxies.length; _j < _len1; _j++) {
          p = proxies[_j];
          if (__indexOf.call(p.commands, command) >= 0) {
            (function(command) {
              var c;
              c = p.proxy(command, prefix, db);
              return proxy[command] = function() {
                var r;
                r = c.apply(db, arguments);
                if (chain) {
                  return proxy;
                } else {
                  return r;
                }
              };
            })(command);
          }
        }
        if (!(command in proxy)) {
          proxyCommand(command);
        }
      }
      return proxy;
    };
  };

}).call(this);
